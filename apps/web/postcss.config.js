export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

/**
 * FILE ROLE:
 * PostCSS configuration to enable Tailwind and Autoprefixer.
 *
 * CONNECTS TO:
 * - tailwind.config.js
 *
 * USED BY:
 * - Vite build pipeline
 */
